# Host UX — wired to sibling cluster-tasks (see bin/wire).
version: "3"

# BEGIN cluster-tasks wire
# Sibling cluster-tasks Taskfile (see CLUSTER_TASKS_ROOT / ../cluster-tasks)
includes:
  ct:
    taskfile: ../cluster-tasks/task/Taskfile.yml
    dir: .
    flatten: true
# END cluster-tasks wire
