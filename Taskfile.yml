# Host UX — cluster-tasks (install).
# App list lives in config/apps.yml — wire/install never overwrites it.
version: '3'

# BEGIN cluster-tasks wire
# Vendored cluster-tasks Taskfile (standalone install — task/cluster-tasks.yml)
includes:
  ct:
    taskfile: task/cluster-tasks.yml
    dir: .
    flatten: true
# END cluster-tasks wire
